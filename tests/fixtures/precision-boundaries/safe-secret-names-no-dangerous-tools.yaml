model: gpt-5
secrets:
  names:
    - OPENAI_API_KEY
    - SLACK_WEBHOOK
    - PROD_TOKEN
tools:
  filesystem:
    enabled: true
    mode: ro
    paths:
      - ./docs
network:
  egress:
    domains: []
